# Submission Artifacts

The `submission/artifacts/` directory contains outputs copied from the validated demo run:

- `symbolic_expression.txt`: SINDy equations generated by `scripts/run_experiment.py` using the submission demo configs.
- `vector_field_parity.png`: parity plot generated from the same demo dataset and trained model.

These artifacts are lightweight demonstration outputs intended to accompany the repository link and run instructions.